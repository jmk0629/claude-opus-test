# /ui-smoke 리포트 — 2026-04-17

> 대상 레포: `/Users/jmk0629/keymedi/medipanda-web` (Playwright 미도입 상태)
> 실행: test-writer 에이전트 1회 (sonnet)

---

## 생성 대상

| 항목 | 값 |
|------|----|
| 원본 문서 | `docs/user/02_HOME.md` |
| 대상 컴포넌트 | `src/pages-user/Home.tsx` |
| 출력 초안 | `reports/ui-smoke/user-02-home.spec.ts` (350줄) |
| 시나리오 수 | 8 (정상 1 / 분기 2 / 액션 2 / 엣지 3) |

---

## 시나리오 커버리지

| # | 설명 | 커버 | 신뢰도 |
|---|------|------|--------|
| 1 | 비로그인 진입 → `hero-public.svg` + `/partner-contract` Link, CSO 섹션 미노출 | 정상/분기 | 🟢 |
| 2 | 로그인 후 통계 3종(처방건수/수수료/오픈병원) 렌더 | 분기 + API mock | 🟡 |
| 3 | API 응답 지연 시 초기 `-` 대시 표시 | 로딩 엣지 | 🟡 |
| 4 | 영업대행 캐러셀 좌/우 화살표 노출 + 클릭 smoke | 액션 | 🟡 |
| 5 | CSO 회원 커뮤니티 탭 전환 (신규처 매칭 ↔ 익명게시판) + 글쓰기 링크 | 액션/권한 | 🟡 |
| 6 | 커뮤니티 0건 시 "게시글이 없습니다." 문구 | 빈 상태 | 🟢 |
| 7 | 캐러셀 API 500 실패 → default svg/jpg fallback | 에러 상태 | 🟢 |
| 8 | 영업대행 캐러셀 5초 자동 전환 | 타이밍 | 🟡 (flaky 위험) |

**신뢰도 판정 기준**
- 🟢: Home.tsx 소스에서 정확 문구/alt/import 경로 확인 완료
- 🟡: 라이브러리 DOM 구조 불명(MedipandaCarousel), 세션 주입 방식 미확정, 한글 텍스트 매칭 의존 등

---

## 실행 준비 (medipanda-web에 Playwright 도입 이후)

```bash
# 1. medipanda-web 루트에서
npm install -D @playwright/test
npx playwright install

# 2. 최소 playwright.config.ts 초안
cat > playwright.config.ts <<'EOF'
import { defineConfig } from '@playwright/test';
export default defineConfig({
  testDir: './e2e',
  use: { baseURL: process.env.BASE_URL ?? 'http://localhost:5174', viewport: { width: 1440, height: 900 } },
  webServer: { command: 'npm run dev:user', url: 'http://localhost:5174', reuseExistingServer: true },
});
EOF

# 3. 초안 spec 복사
mkdir -p e2e
cp /Users/jmk0629/Downloads/homework/claude-opus-test/reports/ui-smoke/user-02-home.spec.ts e2e/

# 4. 실행
BASE_URL=http://localhost:5174 npx playwright test user-02-home
```

---

## 검수 체크리스트 (사람 확인 필수)

- [ ] **세션 주입**: 시나리오 2/3/5/6의 `addInitScript`로 localStorage 주입 가정. 실제 `useSession` 훅 내부가 cookie 기반이면 `storageState` 방식으로 전환.
- [ ] **MedipandaCarousel DOM**: 시나리오 8의 "다음 슬라이드" 판별 기준(transform/aria-hidden/data-index) 실제 DOM 보고 구체화. 현재는 smoke 수준(로드 + 대기)만.
- [ ] **MedipandaButton/Typography role**: `getByRole('link'|'button')`이 실제로 잡히는지 확인. 안 잡히면 텍스트 매칭 또는 `data-testid` 도입.
- [ ] **API 스키마**: `getRecentlyOpenedCount` 반환 타입(스칼라 vs `{ count: n }`) backend.ts에서 재확인. 초안은 스칼라 가정.
- [ ] **한글 텍스트 하드코딩**: "건", "개사", "백만원", "글쓰기" 등 i18n 도입 시 전량 재작성 필요.
- [ ] **viewport**: 히어로 오버레이가 `left: 702px` 절대 위치 — `>=1200px` 보장. config에 `viewport: { width: 1440, height: 900 }` 이미 예시.

---

## ⚠️ 자동 머지 금지

이 초안은 "언젠가 쓸" 시나리오 자료. medipanda-web은 B2 리포트에서 확인된 대로 **e2e 인프라·설정·CI 전무** 상태이므로 현재 동작 검증 불가. Playwright 도입 디데이 결정 → 초안 검수 → e2e 디렉토리 반영 순서로 진행.

---

## 자동화 관점 회고 (C2 자체 검증)

### 작동한 것
- 메뉴 문서의 "API 사용 요약" 표 → `page.route()` 스텁 블록으로 1:1 변환됨 (6개 API 모두 커버)
- Home.tsx 소스 Grep으로 fallback SVG 경로(`default-carousel-*`), 빈 상태 문구("게시글이 없습니다.") 정확 매칭
- 🟡 항목에 `// TODO: verify selector` 주석 자동 삽입 → 검수 대상 가시화
- 350줄 .spec.ts + 요약 한 세션 내 완결, 전 과정 ~100초

### 한계 / 개선 여지
- **UI 라이브러리 DOM 불투명성**: MedipandaCarousel 같은 프로젝트 전용 컴포넌트의 내부 role/class는 문서만으론 추정 불가. 캐러셀 시나리오(8번)는 실제로 사람이 한번 열어보고 data-testid 심는 게 빠름.
- **인증 플로우 불명**: 시나리오 2/3/5/6 모두 localStorage 주입 가정 — 실제 패턴 확정 후 공통 헬퍼로 추출 필요.
- **단일 메뉴 세션**: 현재 커맨드는 한 번에 한 메뉴. 전 메뉴 일괄 생성(admin 9 + user 11 = 20개 초안)하려면 C2.1 배치 모드 추가 가능.
- **검증 루프 부재**: 생성된 spec을 **실제로 파싱·lint하는 단계 없음**. 차기 개선: 생성 직후 `tsc --noEmit`으로 타입 에러만 자동 감지.
